version: '3'
services:
    web:
        image: preservationfrontend
        build:
            context: .
            dockerfile: .docker/Dockerfile
            args:
                buildnumber: 121212
        ports:
            - "80:80"